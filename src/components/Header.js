import React from 'react';
import {Layout} from 'antd';

const {Header} = Layout;

const MainHeader = () => {
  return (
    <Header className="main-header">
      <h2 className="main-logo">MONEI Token</h2>
    </Header>
  );
};

export default MainHeader;
